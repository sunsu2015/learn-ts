/// <reference path="node.d.ts"/>
import * as URL from "url";
import * as Path from "path";
let myUrl = URL.parse("http://www.typescriptlang.org");
let join = Path.join('11111');
console.log(myUrl);
console.log(join);